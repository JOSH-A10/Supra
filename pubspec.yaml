name: vehicle_monitor_app
description: "A beautiful telemetry dashboard for real-time vehicle monitoring."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.7.2

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  firebase_core: ^3.13.0
  firebase_database: ^11.3.5
  syncfusion_flutter_gauges: ^20.3.58
  url_launcher: ^6.2.5
  provider: ^6.1.1
  google_fonts: ^6.1.0
  percent_indicator: ^4.2.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
